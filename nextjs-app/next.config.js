/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove experimental.appDir as it's now stable in Next.js 14
  // Remove api config as it's not a valid Next.js config option
  
  // Environment variables are handled in .env.local
  // No need to explicitly set them in next.config.js
}

module.exports = nextConfig
